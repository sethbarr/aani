# Copy to .env and edit locally. Never commit or paste your API key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4

# Gemini Interactions API. Put a real key in .env, never in this example.
EXTRACTION_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.8-flash
