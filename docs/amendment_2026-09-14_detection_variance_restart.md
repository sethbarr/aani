# Detection variance execution restart

The original predeclaration is commit 3a29ebd8622b3c47af95eb6b9846328a27c86bd8,
committed 2026-09-14T08:15:33-04:00. Its deadline expired while automatic approval
review blocked the transmission to Gemini. Zero model calls, cache attempts, and
draw-start markers exist at restart. The user subsequently explicitly approved
and instructed execution: "approve and run".

This addendum predeclares a fresh two-hour execution window starting
2026-09-14T14:34:12Z and ending 2026-09-14T16:34:12Z. This is a departure from the
original absolute deadline, documented before any draw. The original declaration,
protocol snapshot, runner bytes, and approval-blocked status are retained. The
arm definitions, eight draws, 24 planned calls, order, one-attempt rule, frozen v6
validator, original protocol files, baseline scoring, and hypothesis criteria
remain as originally declared.

The original preservation snapshot inadvertently included a concurrently active,
separate experiment under results/assay_gap_audit, including executable code,
bytecode, caches, and search results. All changed files at restart are in that
unrelated directory. Exclude that directory from the execution preservation
snapshot. Record its excluded paths and old/current hashes for provenance. No
assay-gap audit file is modified by this experiment. Preserve every remaining
original frozen input byte-for-byte, except the experiment runner's deadline and
execution-snapshot lookup. Retain the original runner and freeze its revised bytes
before execution. Add this committed addendum and the experiment tests to the
execution snapshot.

Arm B records remain confined to reader-measurement paths. The experiment does
not invoke a primary or exploratory biological pipeline. The report will state
both declaration commits, the restart timing, every attempted or missing draw,
and any transport failure. Approval waiting time is excluded from this new window.
