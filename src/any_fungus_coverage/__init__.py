"""Aggregate the completed any-fungus tier classification into reports.

This package reads only the cached exploratory retrieval under
data/interim/any_fungus_coverage and reports database coverage. It performs no
retrieval, recomputes no primary output, and never converts coverage into an
activity label.
"""
